# Cycle Management — Unit Queue

Sliced from `docs/superpowers/specs/role-capability/2026-08-03-cycle-management-domain-design.md` by spec-slicer via `spec-cycle-orchestrator`, 2026-08-04. This file is the persisted record of that slice — a fresh session should read this instead of re-running spec-slicer, unless the source spec has changed since.

**Status legend:** `pending` (not started) · `planned` (plan file exists, not yet implemented) · `merged` (implemented, reviewed, on `master`)

| # | Type | Unit | Depends on | Status | Plan file | Merged as |
|---|---|---|---|---|---|---|
| 1 | backend | Admin views cycle history | none | **merged** | `docs/superpowers/plans/2026-08-04-admin-cycle-history.md` | `33e69a7`..`fe3eb54` on `master` |
| 2 | backend | Admin views a single cycle's per-income-type totals | none | **merged** | `docs/superpowers/plans/2026-08-11-cycle-detail-income-totals.md` | `c17bb75`..`0bd2b01` on `master` |
| 3 | backend | Closing a cycle that isn't OPEN is rejected; concurrent close attempts serialize | none | **merged** | `docs/superpowers/plans/2026-08-04-cycle-close-endpoint.md` | `90bfe31`..`50a99ef` on `master` |
| 4 | backend | Closing an OPEN cycle computes leg-volume rollup tree-wide, OPEN→CLOSED, opens next cycle | 3 | **merged** | `docs/superpowers/plans/2026-08-10-cycle-close-legvolume-rollup.md` | `daa1bdc`..`e21bc69` on `master` |
| 5 | backend | Matching Income credited, net of deductions, KYC-gated | 4 | **merged** | `docs/superpowers/plans/2026-08-10-cycle-management-matching-income.md` | `bac5cef`..`0b38f4b` on `master` |
| 6 | backend | Rank advances to highest qualified tier, never demotes | 5 | **merged** | `docs/superpowers/plans/2026-08-10-cycle-management-rank-progression.md` | `0be35d9`..`4552fad` on `master` |
| 7 | backend | Sponsor Matching, one entry per direct sponsee | 5 | **merged** | `docs/superpowers/plans/2026-08-11-cycle-sponsor-matching.md` | `726f907`..`7962728` on `master` |
| 8 | backend | Royalty at post-advancement rank, no-op if no rate configured | 5, 6 | **merged** | `docs/superpowers/plans/2026-08-11-cycle-royalty.md` | `00081d4`..`492ce1e` on `master` |
| 9 | backend | Reward tiers, awarded once ever (no cycle scoping) | 5 | **merged** | `docs/superpowers/plans/2026-08-12-cycle-reward.md` | `e95eb89`..`e693300` on `master` (merge commit `12f1eb0`) |
| 10 | backend | Settlement batch atomic + safely re-runnable end-to-end | 3,4,5,6,7,8,9 | **merged** | `docs/superpowers/plans/2026-08-13-cycle-settlement-batch-verification.md` | `a0113a1`..`315f020` on `master` |
| 11 | **screen** | Admin "Cycle Management" screen — cycle history list (1), per-cycle detail view (2), trigger/monitor close action (4) | 1, 2, 4 | **planned** | `docs/superpowers/plans/2026-08-13-cycle-management-screen.md` | — |

Unit 11 added 2026-08-05, retroactively — added late because screens weren't sliced alongside endpoints the first pass through (see `spec-cycle-orchestrator`'s per-spec loop step 2). One screen unit, not three: the spec's own Admin Screens list names "Cycle Management" as a single screen, and a list/detail/action split across three near-identical Angular pages editing the same route would be the wrong cut, not a more careful one — depends on all three endpoints it surfaces, nothing skipped.

**Excluded (not a unit):** associate-facing endpoint — Decision #13 says none needed, `GET /api/associates/me/dashboard` already covers it. No associate-facing screen for this domain either, for the same reason.

**Open questions carried, not resolved here:** OQ2 (CARRIED_FORWARD reconciliation — lives in Wallet/Withdrawal spec), OQ1 (batch perf/chunking — deferred).

**Next pending unit:** 11 (screen — Admin "Cycle Management" screen) is now **planned**, the only unit left in this spec, plan file persisted at `docs/superpowers/plans/2026-08-13-cycle-management-screen.md`. Depends on 1, 2, 4 — all merged. Unit 10 confirmed (via direct code/test reading, not assumption) that Decisions #1/#2/#3's atomicity/re-run properties were already structurally correct from unit 3 onward, and added the spec's Testing-section coverage that was missing (full hand-computed multi-level-tree pipeline test, realistic mid-batch-failure-then-retry test) — zero production code changed. With 1-10 all merged, `CycleService.close()` is feature-complete for this spec. The unit 11 plan itself flags a real discrepancy worth carrying forward: the merged `CycleCloseResponse` (`cycleId, status, legVolumeRowsWritten, newCycleId`) is narrower than this file's own "screen ... trigger/monitor close action" row and the domain-design spec's aspirational "SettlementResult: entries written per income type, total net amount" language — the plan builds the close-result UI against the real DTO, not the spec prose. The plan also flags (per `spec-cycle-orchestrator`'s per-spec loop step 3b) that a `superpowers:frontend-design` dispatch against the plan's Part A design brief must run and produce `docs/design/admin_operational_screens/cycle_management/{DESIGN.md,screen.png,code.html}` before the plan's Task 7 (its last task) can run — Tasks 1-6 build a fully functional, generically-styled screen first, matching how Sales Register's own screen unit was sequenced.

**Backend history (units 5-10, all merged), kept for reference:** `CycleService.close()` has Matching Income (5), Rank progression (6), Sponsor Matching (7), Royalty (8), and Reward (9) inserted between the leg-volume rollup and the `CLOSED` flip; 9-arg constructor. Unit 8's Task 1 extracted `applyDeductions(...)`/`kycGatedStatus(...)` private helpers, reused by unit 9. Role guard convention: `associate.getRole() == AssociateRole.ASSOCIATE` gates Rank/Royalty only — Sponsor Matching (7) and Reward (9) deliberately have no such guard (apply to Admin too, per Decisions #7/#8). Reward's idempotency check is deliberately cycle-agnostic (`existsByAssociateIdAndIncomeTypeAndSourceRef`, no `cycleId`) since a tier is awarded once ever — don't copy that shape for a cycle-scoped income type. Unit 9 skips zero-`cashReward` tiers entirely (no ledger entry) — a judgement call confirmed with the user, not a spec requirement. Unit 10 (test-only) confirmed atomicity/concurrency/retry (Decisions #1/#2/#3) were already correct and added the spec's missing full-pipeline + realistic-retry test coverage — no production code changed.

**Note for unit 11's plan (the only unit left):** it's a `screen`-type unit — Admin "Cycle Management" screen surfacing endpoints from units 1 (`GET /api/admin/cycles`, history list), 2 (`GET /api/admin/cycles/{id}`, per-cycle detail), and 4/`POST /api/admin/cycles/{id}/close` (trigger/monitor close, response shape from unit 2's endpoint per spec line 97). All three endpoints are merged and stable. Check `docs/design/` and this repo's existing Angular admin screens (e.g. the tree-explorer work already in this repo) for the established token system/responsive rules per `spec-cycle-orchestrator`'s guidance — don't re-decide the palette per screen. No backend file-overlap risk: this unit touches only frontend + the `frontend-design` artifact step.
