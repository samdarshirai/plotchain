# Role Capability & Data Visibility — Unit Queue

Sliced from `docs/superpowers/specs/role-capability/2026-08-03-role-capability-data-visibility-design.md` by spec-slicer via `spec-cycle-orchestrator`, 2026-08-04.

**Reconciled against a pre-existing plan** committed before this slice existed: `docs/superpowers/plans/2026-08-03-role-model-collapse.md` (commit `c949339`, 948 lines) already covers 7 of these 11 units in detail. Where it does, that plan is authoritative — units below point at its task numbers rather than getting a redundant new plan written. Not yet verified line-by-line against this slice's acceptance criteria; do that before executing any of the mapped units, don't assume the mapping is exact.

**Status legend:** `pending` (no plan) · `planned` (plan exists, not implemented) · `merged` (implemented, reviewed, on `master`)

| # | Type | Unit | Depends on | Status | Plan file | Merged as |
|---|---|---|---|---|---|---|
| 1 | backend | Only ADMIN role carries back-office authority | none | merged | `2026-08-05-role-capability-unit-1-admin-only-authority.md` (own plan, written from scratch — verified `role-model-collapse.md` Tasks 2-3 had drifted, e.g. 18 vs. stated 14 `SecurityConfig` occurrences) | `857a27d..5befd78` |
| 2 | backend | Admin seeded as single root account via Flyway migration | none | merged | `2026-08-13-role-capability-unit-2-admin-seed-migration.md` (own plan, written from scratch — found V16→V18 migration-filename drift and two real `user_id='admin'` fixture collisions the reference plan and spec both missed) | `63ce818..232c00d` |
| 3 | backend+removal | Root Associate no longer exists as separate account/step | 2 | merged | `2026-08-14-role-capability-unit-3-root-associate-removal.md` (own plan, written from scratch against post-unit-4 repo state — reference plan's Task 5/9 step numbers were stale) | `660d3b9..2639198` |
| 4 | backend+removal | Admin Team setup step removed | 1 | merged | `2026-08-14-role-capability-unit-4-admin-team-removal.md` (own plan — empirically verified expected-red baseline drop 14→4, found frontend drift the reference plan never covered) | `ae8266b..ccdfa6e` |
| 5 | backend | Associate can view their own subtree | none | merged | `2026-08-13-role-capability-unit-5-associate-own-subtree.md` (own plan — correctly left `TreeExplorerService` untouched since units 2/3's "Admin becomes tree root" isn't merged yet) | `92f3e4e` |
| 6 | backend | Associate can view available plots | 1 (verify overlap — see original slice's open question) | merged | `2026-08-13-role-capability-unit-6-associate-view-plots.md` (own plan — expanded test coverage beyond the reference plan) | `a69a5d1..0811eeb` |
| 7 | backend | Plot booking + EMI schedule runtime | 6 | merged | `2026-08-14-role-capability-unit-7-plot-booking-emi.md` (own plan, written from scratch — new `booking` package, mirrors Sales' row-lock/status-flip pattern) | `caaa4ca..3421abc` |
| 8 | backend | Associate can submit KYC + view own status | none | merged | `2026-08-13-role-capability-unit-8-associate-kyc-submission.md` (own plan — originally clashed with unit 2 on migration number `V18`, renumbered to `V19` at the orchestrator checkpoint) | `a647eaa..97ee872` |
| 9 | backend | Associate can view own rank progress / reward tiers | none | merged | `2026-08-13-role-capability-unit-9-associate-rank-progress.md` (own plan, written from scratch — no pre-existing plan covers this unit) | `c0fbf10..fd53e02` |
| 10 | backend | Associate can view their digital ID card | none | merged | `2026-08-13-role-capability-unit-10-associate-digital-id-card.md` (own plan, written from scratch — photo stubbed null, QR is a raw payload string, no new dependency) | `a2fb675..568d59b` |
| 11 | backend | Associate can view and edit their own profile | none | merged | `2026-08-13-role-capability-unit-11-associate-profile-edit.md` (own plan — widened scope to include email, correctly excluded bank details as a real data-model gap) | `69ae850..eba4873` |
| 12 | **screen** | Associate "My Tree" screen | 5 | merged | `2026-08-14-role-capability-unit-12-my-tree-screen.md` (own plan — reuses admin/tree-explorer's TreeNode model + layout/pan-zoom math by cross-folder import, no admin files modified) | `cdaa6a1..601f1a3` |
| 13 | **screen** | Associate "Plot Bookings + EMI schedule" screen | 6, 7 | merged | `2026-08-14-role-capability-unit-13-plot-bookings-emi-screen.md` (own plan — strictly view-only, resolved a scope conflict against Admin-only booking creation) | `ed852a6..da3666a` |
| 14 | **screen** | Associate "Profile & Bank/KYC Details" screen (the one editable screen — submit KYC docs, edit profile) | 8, 11 | merged | `2026-08-14-role-capability-unit-14-profile-kyc-screen.md` (own plan — bank details deliberately inert/disabled, no invented backend fields) | `d7f7f6d..ab91e99` |
| 15 | **screen** | Associate "Rewards & Perks progress" screen | 9 | **merged** | `docs/superpowers/plans/2026-08-14-role-capability-unit-15-rewards-screen.md` | `8da9b91..cb2b9d9` on `master` |
| 16 | **screen** | Associate "Digital ID Card" screen | 10 | pending | — | — |

Units 12–16 added 2026-08-05, retroactively, same reason as cycle-management unit 11 — screens weren't sliced alongside endpoints the first pass. Grouped by the spec's own Associate Screens list, not 1:1 per endpoint: units 8 (KYC submission) and 11 (profile edit) share one screen unit (14) because the spec's own screen list names them as one screen — "Profile & Bank/KYC Details (the one editable screen)" — not two. Same reasoning for 13 (plots-view + booking/EMI share one "Plot Bookings" screen). Units 1–4 get no screen unit: 1–2 are pure backend/authority changes with no new screen surface, and 3–4 are *removals* of existing setup-wizard frontend, already inside those units via Task 9 — not a new screen to build.

**Skipped (already aligned, no unit needed):** Dashboard/stats, Associate directory, Audit log — see original slice.

**Before executing units 1–6 or 11:** read `role-model-collapse.md`'s relevant task(s) in full and confirm its acceptance criteria actually satisfy this slice's — it was written independently of this slice and may diverge in scope or convention (e.g. check it against this repo's current `SecurityConfig.java`/`AssociateRole` state, which may have moved since `c949339`). Confirmed by direct read (2026-08-05): Tasks 7 and 8 are backend-only (new endpoints, no Angular work) — units 12 and 14's screen work is not secretly already done by this plan.

**Units 7–10 need a plan written from scratch** — nothing pre-existing covers them. **Units 12–16 (all screens) need a plan written from scratch too** — the pre-existing plan never touches the frontend beyond deleting the two setup-wizard steps.

**Unit 1 merged 2026-08-13 (`857a27d..5befd78`), executed strictly in isolation** (not combined with units 2-4, per explicit choice at the orchestrator checkpoint despite the pre-existing reference plan treating Tasks 1-5 as one interlocked sequence). Its own plan documented 8 expected-residual-red test methods (Admin Team/Root Associate routes, resolved only when unit 4 deletes those controllers) — `mvn test` after merge confirmed exactly those 8 plus 2 more with an identical root cause the plan's table omitted (`AdminControllerTest.createReturnsCreatedWithAOneTimePassword`, `AdminProvisioningServiceTest.listExcludesAssociateRows`), plus 4 fully unrelated pre-existing failures (`JwtServiceTest`/`SecretsEncryptionServiceTest` — a latent no-op fail-closed check, confirmed via git log as untouched by this unit). **Before planning unit 4 (Admin Team removal): fold both table-gap tests into its own expected-red accounting so they aren't mistaken for new regressions.**

**Unit 5 merged 2026-08-13 (`92f3e4e`)** — clean review, no new residual-red tests, no `SecurityConfig` change.

**Unit 9 merged 2026-08-13 (`c0fbf10..fd53e02`)** — review caught one real Standards violation (a duplicate global `AssociateNotFoundException` handler added to `CompensationExceptionHandler`, when `DashboardExceptionHandler` already owns that mapping app-wide — this codebase has already written the "don't do this" rule down in `SalesExceptionHandler`'s own header comment) and one minor test-coverage gap (no exact-threshold-equality case for the reward-tier `achieved` predicate, inherited from the plan's own test snippets). Both fixed before merge (`fd53e02`). No new migration, no `SecurityConfig` matcher, no residual-red change — independently spot-checked the `CycleClose*IntegrationTest` classes in this unit's worktree too (unlike unit 2, this one was clean, no regression).

**Unit 2 merged 2026-08-13 (`63ce818..232c00d`) — caught and fixed a real regression before merge.** Its implementing agent initially misclassified 3 failing `CycleClose*IntegrationTest` tests as "unrelated in-flight cycle-management work" — verified false by running the same 3 classes against a clean `master` baseline directly (`BUILD SUCCESS`, 4/4 green there, 2 failures + 1 error in the unit-2 worktree). Root cause: `CycleService.close()` calls `associateRepository.findAll()` unscoped, and the new `V18__seed_founding_admin.sql` migration's permanent `ADMIN`-role root row (no children) gets swept into every `@SpringBootTest` integration test's leg-volume rollup, shifting count-based assertions off by exactly one (`expected: 8 but was: 9`, `expected: 6L but was: 7L`) and leaving an orphaned `leg_volume` row `CycleCloseConcurrencyTest.cleanUp()` didn't account for on delete. Fix (`232c00d`, code-review-verified correct against the spec's own "Resolved decisions #2" — the engine including the root's own zero-volume leg-volume row is intended behavior, not a bug): scoped the 3 tests' assertions to their own fixture associate IDs instead of counting unscoped, and fixed `cleanUp()`'s delete order (leg_volume before cycle) to match the pattern the other two integration tests already used. **Lesson for future units that seed permanent rows into every environment: always diff integration-test results against a clean master baseline before trusting an implementer's "unrelated/pre-existing" classification — don't take it on faith even when the reasoning sounds plausible.**

**Unit 8 merged 2026-08-13 (`a647eaa..97ee872`)** — clean review, 0 spec findings. Confirmed `V19` (not `V18`) used correctly, no double-registered exception handler (unlike unit 9's first pass), self-scoping and every design decision (BYTEA storage, overwrite-on-resubmit, unconditional PENDING reset, no audit log) verified directly against the code. One judgement call surfaced and explicitly kept as-is: KYC uploads stay unaudited like `AuthService.changePassword`, even though `KycReviewService.decide()` (the closer admin-side sibling) does audit-log — user's call, not changed.

**Unit 6 merged 2026-08-13 (`a69a5d1..0811eeb`) — hit a real process bug, now fixed for good.** Its plan was approved but never `git commit`ed before the worktree was created; a worktree is a separate checkout and can't see uncommitted files in the main working tree, so the implementer's worktree silently fell back to the older `role-model-collapse.md` Task 6 reference plan for its first pass — production code matched either way (identical matcher split), but test coverage was narrower than approved (missing 6 of 8 documented test cases). Caught by diffing the implemented tests against the actually-approved plan file; fixed in a follow-up commit (`0811eeb`) adding the missing 6. Review after the fix found only cosmetic issues (a stale comment-chain reference, test-pairing layout scattered by the two-pass fix, one dropped/one paraphrased explanatory comment) — no functional gaps, merged as-is. **Process fix applied going forward, saved to memory (`spec_cycle_commit_plans_before_worktree`): commit a plan file immediately on approval, before dispatching its worktree TDD agent — don't defer to merge time.**

**Unit 11 merged 2026-08-14 (`69ae850..eba4873`)** — clean review both axes, 0 hard findings. Plan file was committed before dispatch this time (the process fix held) — implementation had zero drift from the plan, no duplicate exception handler, same-email-no-op edge case correctly handled and tested.

**Unit 10 merged 2026-08-14 (`a2fb675..568d59b`)** — clean review both axes, 0 hard findings, zero drift from plan. Closes out this batch (6, 10, 11) — all three sequenced correctly on `SecurityConfigTest.java` overlap, no conflicts.

**Unit 4 merged 2026-08-14 (`ae8266b..ccdfa6e`)** — pure removal, backend expected-red baseline dropped from 14 to exactly 4 as the plan predicted (the 10 leftover from unit 1's incomplete enum collapse are gone for good — `AdminController`/`AdminProvisioningService`/their tests deleted wholesale). Frontend 489/489 green. Found and fixed 5 real plan gaps beyond the committed plan's file list (stale constructor arg counts, stale comment references, hardcoded step/nav-rail counts in files the plan never listed, an orphaned 342-line SCSS block) — each in its own `fix(...)` commit. Review surfaced 3 more cosmetic-only nits (a dangling comment reference, one undisclosed comment-only touch near unit 3's territory, an inert stale `'ADMIN_TEAM'` value in a migration CHECK constraint) — none functional, merged as-is. Root Associate (unit 3's territory) confirmed fully untouched beyond the one authorized step-number change.

**Unit 7 merged 2026-08-14 (`caaa4ca..3421abc`)** — clean review both axes, 0 spec findings. New `com.plotchain.booking` package built as a close, deliberate mirror of the Sales domain's precedent (row-locked status flip, Admin-creates/Associate-reads split). Verified: flat equal-split EMI math with remainder absorption on the last installment, `PlotRepository.findByIdForUpdate` row lock closes the double-booking race (dedicated concurrency test), self-scoped to caller only (not self+downline, per the spec's narrower wording for this row), no duplicate exception handler. Merged into master alongside unit 4's concurrent removal work with a clean auto-merge on the shared `SecurityConfig.java`/`SecurityConfigTest.java` files.

**Unit 3 merged 2026-08-14 (`660d3b9..2639198`)** — pure removal, plan derived fresh from post-unit-4 repo state (`SetupStateService` 7→6 steps, `reviewLaunch` renumbered to 6). Implementer found and fixed 2 real drift items the plan's own file list missed: stale 5-null constructor tails (+ a dangling comment naming the deleted field) in `AuthControllerTest`/`AuthServiceTest`, and a hardcoded `jsonPath("$.steps.length()").value(7)` in `SetupStateControllerTest` (a controller-level test distinct from `SetupStateServiceTest`, not in the plan's list at all). Code review caught one more real gap: ~250 lines of orphaned `.root-associates-step__*` CSS in `frontend/src/styles/_setup.scss` — untouched by the diff because the plan's own verification grep (Task 3 Step 13) was scoped to `--include="*.ts/*.html/*.json"`, structurally excluding `.scss`. Fixed in a follow-up commit (`2639198`) after an unscoped grep confirmed nothing else was missed. Backend 584/588 (4 pre-existing JDK21/25 env failures, independently verified), frontend 473/473, both matching prediction exactly post-merge. **Lesson: a plan's own "verify nothing was missed" grep can itself be under-scoped — spot-check its `--include` filters cover every file type actually touched by the removal (CSS included), don't just trust the implementer replaying the plan's literal grep command.**

**Units 12–16 (screens) planned 2026-08-14, all in parallel** (`frontend-design` skill unavailable in this environment; agents planned directly off existing frontend patterns + the merged backend contracts instead). 3 of 5 planning agents (12, 13, 16) hit the session API limit mid-write and were resumed via SendMessage — all had already finished writing before the interruption landed, no lost work. Two real scope corrections surfaced during planning: unit 13's plan initially risked including a booking-creation form, but the backend (`POST /api/admin/bookings` is `ADMIN`-only, 403s an Associate token) and spec both say booking creation is Admin-on-behalf-of-associate — resolved to a strictly view-only screen, flagged the missing Admin-side booking-creation screen as a real backlog gap (not in this unit's scope). Unit 14 flagged the bank-details spec/backend gap explicitly (disabled "not collectible yet" banner, no invented fields) rather than silently dropping the "Bank" half of the screen's own name. Unit 16 flagged two real gaps versus the spec's literal wording rather than guessing at fixes: QR renders as visible text (no QR library exists in `package.json`), photo renders as initials (no photo-upload path exists anywhere).

**Unit 12 merged 2026-08-14 (`cdaa6a1..601f1a3`)** — clean review both axes, 0 hard findings. Review caught one real-but-cheap gap the plan's own file list omitted: `app.routes.spec.ts` had no guard test for the new `/my-tree` route (the exact property — self-scoping/access control — this unit is about). Fixed in a follow-up commit (`601f1a3`) verified TDD-style (temporarily dropped the guard, confirmed the new test failed for the right reason, restored it). 483/483 frontend, zero backend touch, admin `tree-explorer` files confirmed untouched by the reuse.

**Unit 13 merged 2026-08-14 (`ed852a6..da3666a`)** — clean review both axes, 0 findings above low severity. Implementer proactively added the `/plot-bookings` guard test to `app.routes.spec.ts` from the start (unit 12's review gap fed forward correctly, no repeat). Plan's own nav/route snippets were stale by the time this unit implemented (written before unit 12/My Tree existed) — implementer adapted correctly rather than pasting blindly, inserting `plot-bookings` after the now-existing `my-tree` entry and updating the nav-link assertion to the real 4-item array; review verified `app.routes.ts` order, DOM order, and the test assertion all agree with each other. View-only scope boundary independently verified by grep (zero `.post/.put/.delete/.patch/<form>` in the new package) — booking creation stays Admin-only per the backend, as unit 13's plan decided. 499/499 frontend, no backend touch.

**Unit 14 merged 2026-08-14 (`d7f7f6d..ab91e99`)** — review caught one real functional bug the plan's own Task 3 code carried: `saveError` was set on a non-409 save failure but never bound in the template, so a 500 (or an unmapped 400) failed completely silently with no user-visible feedback. Fixed in a follow-up commit (`ab91e99`), TDD-verified RED (missing DOM node) before GREEN. Two other review findings were correctly identified as non-issues: `hi.json`'s English `profileKyc.*` body copy matches the established nav-only-real-Hindi convention (not a bug), and the missing local-file-preview is an already-documented, already-accepted plan gap in the same category as the deliberately-inert bank-details section and the no-thumbnail-for-submitted-docs gap. Nav/route insertion correctly adapted to the current (post-12/13) state rather than the plan's stale snippets; a pre-existing `app.routes.ts`/`app.component.html` ordering discrepancy between `my-tree`/`sales-history` (introduced by units 12/13, not this one) was independently confirmed via `git blame` to be unrelated to this unit. 518/518 frontend, no backend touch.
