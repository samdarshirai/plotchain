services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: plotchain
      POSTGRES_PASSWORD: plotchain
      POSTGRES_DB: plotchain
    ports:
      - "5434:5432"
    volumes:
      - plotchain-db-data:/var/lib/postgresql/data

volumes:
  plotchain-db-data:
